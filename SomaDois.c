#include <stdio.h>
#include <math.h>
#include <conio.h>
int main() {
    int A, B, SOMA;
    scanf("%i", &A);
    scanf("%i", &B);
    SOMA = A + B;
    printf("SOMA = %i", SOMA);
    
    return 0;
}